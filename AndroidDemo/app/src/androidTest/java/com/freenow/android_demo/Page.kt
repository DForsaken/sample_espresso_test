package com.freenow.android_demo

interface Page {
    /*
    * Quick verification that the current page is the expected
    */
    fun verifyPage() {
        // override with own needs. Note do not add more than 2 elements preferably
    }
}
